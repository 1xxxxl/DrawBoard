10.21：
	shape中不再用两点坐标代表该shape位置，改为一个储存POINT的vector
	shape中新加入成员变量point_num表示vector中点的个数
	更改文件存储和打开时对文档中点坐标的读写逻辑

	(注：在用POINT* point储存点坐标时，被pushback到vector中后需要point = new POINT否则会指向同一块区域)

	增加Polygon类
	ifin函数的实现通过射线法实现(做一条射线，与多边形的各个边交点个数为奇数则在内部)
	通过两次点击事件的时间间隔和距离间隔判断是否是双击(代表结束绘制)

10.26：
	在只需要两个点就能绘制的图形中，通过判断点击事件的down和up的时间间隔和距离间隔判断是否采用拖动绘制(两种间隔均大于规定值时判定为拖动绘制)否则采用点击绘制

